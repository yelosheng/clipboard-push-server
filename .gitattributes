# Shell scripts must keep LF endings. A CRLF entrypoint fails inside the
# container with "bad interpreter: /bin/sh^M", which is a confusing way to
# find out your checkout was on Windows.
*.sh text eol=lf
Dockerfile text eol=lf
